/target
/temp

# pi agent local state (trust / cache / local settings)
.pi/

# Editor / OS noise
.DS_Store
*.swp
.idea/
.vscode/
# Local deck working dirs from pptx<->pptd round-trip sessions
ai-agent-deck/
